# ETHINICITY-DETECTION-MODEL
The repository implements the VGG16 (Visual Geometry Group 16) Face Model Architecture for race classification. The model is trained on the FairFace dataset using techniques such as transfer learning, one-hot encoding, and early stopping.
